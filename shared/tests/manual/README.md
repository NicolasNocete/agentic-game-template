# Pruebas Manuales

Registra un archivo por flujo importante usando `test-case-template.md`. Cada caso debe incluir el commit o build evaluado, pasos reproducibles, resultado esperado, resultado observado y estado.

Como minimo, antes de una entrega registra los flujos de inicio, core loop, exito, fallo y reinicio cuando existan en el juego.
