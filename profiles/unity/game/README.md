# Inicializacion Unity

Este perfil prepara la estructura y reglas de versionado. Crea o abre esta carpeta desde Unity Hub con la version acordada por la materia. Unity generara los archivos de `ProjectSettings/` y los `.meta` necesarios; deben versionarse despues de comprobar que el proyecto abre.
