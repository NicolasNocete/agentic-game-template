# Inicializacion Unity

Este perfil prepara la estructura y reglas de versionado. Registra primero la version exacta acordada en `docs/project-manifest.md` y crea o abre esta carpeta desde Unity Hub con esa version. Unity generara los archivos de `ProjectSettings/` y los `.meta` necesarios; deben versionarse despues de comprobar que el proyecto abre.
