---
description: Preparar o analizar un playtest de una experiencia jugable.
agent: general
---

Load the `playtest-facilitator` skill.

Prepara o analiza este playtest:

$ARGUMENTS

Usa `docs/vertical-slice-contract.md` y registra el resultado en `docs/production/playtests.md` si el usuario confirma que quiere guardarlo. Las tareas deben ser observables y no inducir respuestas.
