---
description: Crear o actualizar una especificacion antes de implementar una funcionalidad relevante.
agent: general
---

Load the `create-game-spec` skill.

Crea o actualiza la spec relevante para esta solicitud:

$ARGUMENTS

Usa las reglas de `AGENTS.md` y el alcance de `docs/vertical-slice-contract.md`. Elige una spec rapida, de gameplay, UI, nivel o tecnica. Para cambios triviales, explica por que basta una tarea breve.
