---
description: Planificar e implementar una tarea o spec con verificacion manual.
agent: build
---

Load the `create-game-spec` skill when the task needs clarification.

Implementa esta solicitud:

$ARGUMENTS

Primero lee la spec, tarea y manifest relevantes. Para cambios no triviales, presenta un plan tecnico breve y espera confirmacion antes de editar. Mantiene el alcance de la vertical slice, explica decisiones en espanol y realiza una verificacion manual enfocada al finalizar.
