---
description: Crear un proyecto academico desde un perfil Godot, Unity o Phaser.
agent: build
---

Load the `game-project-bootstrap` skill.

Inicializa este proyecto con el motor indicado:

$ARGUMENTS

Trabaja en la copia del estudiante, no en un repositorio fuente compartido. Copia `shared/` y el perfil elegido desde `profiles/` a la raiz. Conserva `.opencode/` completo. El resultado debe contener solo el perfil tecnico elegido en `game/`; no conserves perfiles de otros motores.

Antes de cambiar archivos, confirma si ya existe un proyecto inicializado. No sobrescribas trabajo existente. Al finalizar, explica en espanol los pasos manuales pendientes y pide ejecutar `/guide`.
